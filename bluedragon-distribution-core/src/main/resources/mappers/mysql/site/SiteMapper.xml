<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.jd.bluedragon.external.gateway.dao.SiteMapper">


    <select id="getByOrgIdAnd" resultType="com.jd.bluedragon.external.gateway.domain.Site" >
        select site_code, site_name
        from site
        where
         org_id =#{orgId}

        <if test="siteCode!=null">
           and site_code=#{siteCode}
        </if>

        <if test="siteName!=null">
            and site_name like concat(concat('%',#{siteName}),'%')
        </if>

        and site_type in
        <foreach collection="siteTypeList" item="siteTypeItem" open="(" close=")" separator=",">
            #{siteTypeItem}
        </foreach>


    </select>
</mapper>